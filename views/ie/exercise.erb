<div class="container-white">
	<div class="container">
		<div class="row" style="padding: 20px 0 24px 0;">
			<div class="span12">
				<a href="/student/reports/report/enter_score" class="btn btn-small pull-right">Back</a>
				<h1>Career Direction Interest Exercise</h1>
				<hr />
			</div>
			<div class="span8 offset2">
			<table class="table table-striped table-condensed">
				<h3>As part of my job, I would enjoy...</h3>
					
			<% @ie.each do |ie| %>
				<tr>
					<td style="text-align: center;"><%= ie.position_number %></td>
					<td><%= ie.item %></td>
					<td style="text-align: right;">
						<label class="radio inline checker">YES
  						 	<input type="radio" name="<%= ie.position_number %>" id="optionsRadios<%= ie.position_number %>" value="option1">
							<span class="checkmark"></span>
						</label>
						<label class="radio inline checker">NO
  						 	<input type="radio" name="<%= ie.position_number %>" id="optionsRadios<%= ie.position_number %>" value="option2">
							<span class="checkmark"></span>
						</label>
					</td>
				</tr>
			<% end %>
			</table>
			<input class="btn btn-small btn-success pull-right" style="margin-left: 2em;" type="submit" value="Save" />
			<h5 class="pull-right">Save to view your Career Direction 2 Student Report</h5>
		</div>
	</div>
</div>

				
<style>
/* The container */
.checker {
  display: block;
  position: relative;
  padding-left: 30px;
  margin-bottom: 22px;
  color: gray;
  cursor: pointer;
  font-size: 13px;
  line-height: 1em;
  -webkit-user-select: none;
  -moz-user-select: none;
  -ms-user-select: none;
  user-select: none;
}

/* Hide the browser's default radio button */
.checker input {
  position: absolute;
  opacity: 0;
  cursor: pointer;
}

/* Create a custom radio button */
.checkmark {
  position: absolute;
  top: 0;
  left: 0;
  height: 20px;
  width: 20px;
  background-color: white;
  border: 1px solid gray;
  border-radius: 50%;
}

/* On mouse-over, add a grey background color */
.checker:hover input ~ .checkmark {
  background-color: #e4e4e4;
}

/* When the radio button is checked, add a blue background */
.checker input:checked ~ .checkmark {
  background-color: #2196F3;
  border: 1px solid #2196F3;
}

/* Create the indicator (the dot/circle - hidden when not checked) */
.checkmark:after {
  content: "";
  position: absolute;
  display: none;
}

/* Show the indicator (dot/circle) when checked */
.checker input:checked ~ .checkmark:after {
  display: block;
}

/* Style the indicator (dot/circle) */
.checker .checkmark:after {
 	top: 6px;
	left: 6px;
	width: 8px;
	height: 8px;
	border-radius: 50%;
	background: white;
}
</style>				