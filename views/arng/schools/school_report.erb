	<div class="container">	
		<div class="row">  
			<div class="col-md-12">
				
				<% if session[:recruiter] %>
					 <a href="/recruiters/<%= session[:recruiter] %>/profile" style="margin-top: 12px;" class="btn pull-right">Recruiter Account</a>
				<% end %>
				
				
				<h1 class="page-header">Class Reports</h1>
			</div>
			
			<div class=" col-md-5">
					<h5><%= @school.school_name %></h5>
					<p><%= @school.school_address1 %> <%= @school.school_address2 %>
						<br />
						<%= @school.school_city %>, <%= @school.school_state %> &nbsp;<%= @school.school_zip %>
					</p>
					<p>
						<strong>School Password:</strong> &nbsp; <%= @school.school_password %><br />

						<strong>Recruiter:</strong> &nbsp; <%= @school.recruiter.rank %> <%= @school.recruiter.first_name %> <%= @school.recruiter.middle_name %> <%= @school.recruiter.last_name %><br />
					</p>
			</div>		
			<div class=" col-md-7">
				
				<form method="get">
					<p>
						<strong>Select class date</strong>
					</p>
					<p>
						<span style="float:left;"><%= date_to_field('start', @start) %></span><button type="submit" class="btn btn-primary pull-left" style="margin-left: 6px; margin-top: 2px;">Show</button>
					</p>
				</form>				
				
				
				<p class="clear-both">&nbsp;<br /><br /></p>
				
				
				<div class="row" style="margin-top: 2em;">
					<div class="col">

						<p><strong>Summary Report</strong></p>
						
						<p><a href="/arng/schools/<%= @school.id %>/summary_report" class="btn btn-success">Export</a></p>
						
						<!-- <p><a href="<%= request.url %><%= params.count > 0 ? '&export=true' : '?export=true' %>" class="btn btn-success" >Export</a>
						</p> -->
					</div>
					<form method="post">
					<div class="col">
						<p><strong>Individual Reports</strong></p>
						<p><a href="/arng/schools/<%= @school.id %>/ind_report" class="btn btn-success" >Export</a></p>
					</div>
					</form>
				</div>
			</div>
		</div>
		<div class="row"> 
			<div class="col-md-12">	
				
				<br />
				
				<table class="table table-sm table-striped table-condensed table-hover">
					<tr class="table-success">
						<th>Student &nbsp;&nbsp;<small>[ <%= @school.students.count %> ]</small></th>
						<th>School Password</th>
						<th>Class Date</th>
					</tr>
					
					<% @school.students.sort_by(&:created_on).reverse.each do |student| %>
						
					<tr>
						<td>
							<%= student.first_name %> <%= student.middle_name %> <%= student.last_name %>
						</td>
						<td>
							<%= student.school_password %>
						</td>
						<td> 
							<%= format_american_day(student.created_on) %>
						</td>
						
					</tr>
						
					<% end %>
				</table>

				<!-- <table class="table table-sm table-striped table-condensed table-hover">
						<tr class="table-success">
							<th>Student &nbsp;&nbsp;<small>[ <%= @school.students.count %> ]</small></th>
							<th>School Password</th>
							<th>Class Date</th>
						</tr>
						<% @school.students.sort_by(&:last_name).each do |student| %>
						<% if @school.class_date = params[:class_date] %>

						<tr>
							<td>
								<%= student.first_name %> <%= student.middle_name %> <%= student.last_name %>
							</td>
							<td>
								<%= student.school_password %> - <%= student.class_date %>
							</td>
							<td>
								<%= student.created_on %> - <%= @school.class_date %>
							</td>

						</tr>

						<% end %>
						<% end %>
					</table> -->
				<!-- <%= session[:flash] = params.inspect %> -->
			</div>
		</div>
		
	</div>

